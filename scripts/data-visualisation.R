### Load packages

# Load pacman and install the required packages
library(ggplot2)
library(readr)

#####Load in the data###############################
gapminder <- read_csv("data/gapminder_data.csv")
View(gapminder)
